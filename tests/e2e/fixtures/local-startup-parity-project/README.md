# Local Startup Parity Fixture

Fixture reserved for tests that compare local startup behavior against CLI entrypoint behavior.

Like other fixtures, this is copied into a temp workspace and receives the built plugin via symlink injection.
