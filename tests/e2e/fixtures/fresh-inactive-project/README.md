# Fresh Inactive Fixture

Represents a brand-new project with no `.coder/` state.

Expected behavior in scenario tests:

- plugin remains in init-only mode
- startup should not create active-management artifacts unless explicitly enabled
