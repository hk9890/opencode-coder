# CLI Smoke Fixture

Minimal fixture for running real OpenCode CLI smoke commands (for example `opencode run ...`).

This project is intentionally lightweight and does not assume prior plugin activation state.
